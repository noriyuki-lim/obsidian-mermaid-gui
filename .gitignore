node_modules
*.log
.DS_Store
coverage

# esbuild deliverables — generated by `npm run build`
main.js
main.js.map
styles.css
*.tsbuildinfo

# legacy snapshot of the web app, kept for reference; not part of the plugin
_legacy/

# internal backlog / planning notes — not part of the public plugin
backlog/

# ad-hoc distribution zips (built locally for manual handoff)
*.zip

# internal dev docs / AI-agent instructions — not part of the public plugin
# (kept on disk locally; just no longer version-controlled/pushed)
docs/
AGENTS.md
CLAUDE.md
