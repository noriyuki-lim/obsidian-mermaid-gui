// positions-codec removed: GUI positions are no longer persisted to Mermaid source.
// stripGuiComments (backward-compat strip of legacy %% gui:* comments) is covered by parser tests.
import { describe, it } from "vitest";
describe("positions-codec (removed)", () => {
  it.todo("module removed; legacy %% gui:* strip covered by stripGuiComments in parser.test.ts");
});
